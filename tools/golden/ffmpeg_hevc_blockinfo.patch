diff --git a/libavcodec/hevc/hevcdec.c b/libavcodec/hevc/hevcdec.c
index 7bb4fe9..2cb672e 100644
--- a/libavcodec/hevc/hevcdec.c
+++ b/libavcodec/hevc/hevcdec.c
@@ -77,10 +77,15 @@ static void pic_arrays_free(HEVCLayerContext *l)
 
     av_freep(&l->skip_flag);
     av_freep(&l->tab_ct_depth);
+    av_freep(&l->tab_part_mode);
+    av_freep(&l->tab_cu_bits);
+    av_freep(&l->tab_pu_bits);
+    av_freep(&l->tab_tu_bits);
 
     av_freep(&l->tab_ipm);
     av_freep(&l->cbf_luma);
     av_freep(&l->is_pcm);
+    av_freep(&l->tab_tu_log2);
 
     av_freep(&l->qp_y_tab);
     av_freep(&l->tab_slice_address);
@@ -117,15 +122,26 @@ static int pic_arrays_init(HEVCLayerContext *l, const HEVCSPS *sps)
     if (!l->sao || !l->deblock)
         goto fail;
 
-    l->skip_flag    = av_malloc_array(sps->min_cb_height, sps->min_cb_width);
-    l->tab_ct_depth = av_malloc_array(sps->min_cb_height, sps->min_cb_width);
-    if (!l->skip_flag || !l->tab_ct_depth)
+    l->skip_flag     = av_malloc_array(sps->min_cb_height, sps->min_cb_width);
+    l->tab_ct_depth  = av_malloc_array(sps->min_cb_height, sps->min_cb_width);
+    l->tab_part_mode = av_malloc_array(sps->min_cb_height, sps->min_cb_width);
+    if (!l->skip_flag || !l->tab_ct_depth || !l->tab_part_mode)
+        goto fail;
+
+    l->tab_cu_bits = av_calloc(sps->min_cb_height * sps->min_cb_width,
+                               sizeof(*l->tab_cu_bits));
+    l->tab_pu_bits = av_calloc(sps->min_cb_height * sps->min_cb_width,
+                               sizeof(*l->tab_pu_bits));
+    l->tab_tu_bits = av_calloc(sps->min_cb_height * sps->min_cb_width,
+                               sizeof(*l->tab_tu_bits));
+    if (!l->tab_cu_bits || !l->tab_pu_bits || !l->tab_tu_bits)
         goto fail;
 
     l->cbf_luma = av_malloc_array(sps->min_tb_width, sps->min_tb_height);
     l->tab_ipm  = av_mallocz(min_pu_size);
     l->is_pcm   = av_malloc_array(sps->min_pu_width + 1, sps->min_pu_height + 1);
-    if (!l->tab_ipm || !l->cbf_luma || !l->is_pcm)
+    l->tab_tu_log2 = av_malloc_array(sps->min_tb_width, sps->min_tb_height);
+    if (!l->tab_ipm || !l->cbf_luma || !l->is_pcm || !l->tab_tu_log2)
         goto fail;
 
     l->filter_slice_edges = av_mallocz(ctb_count);
@@ -1309,6 +1325,19 @@ static int hls_transform_unit(HEVCLocalContext *lc,
     const int log2_trafo_size_c = log2_trafo_size - sps->hshift[1];
     int i;
 
+    /* VideoEye: record this leaf's luma TU size for every min-TB it covers. */
+    if (l->tab_tu_log2) {
+        int log2_tb = sps->log2_min_tb_size;
+        int span    = 1 << (log2_trafo_size - log2_tb);
+        int x_tb    = x0 >> log2_tb;
+        int y_tb    = y0 >> log2_tb;
+        int ty, tx;
+        for (ty = 0; ty < span && (y_tb + ty) < sps->min_tb_height; ty++)
+            for (tx = 0; tx < span && (x_tb + tx) < sps->min_tb_width; tx++)
+                l->tab_tu_log2[(y_tb + ty) * sps->min_tb_width + (x_tb + tx)] =
+                    (uint8_t)log2_trafo_size;
+    }
+
     if (lc->cu.pred_mode == MODE_INTRA) {
         int trafo_size = 1 << log2_trafo_size;
         ff_hevc_set_neighbour_available(lc, x0, y0, trafo_size, trafo_size, sps->log2_ctb_size);
@@ -2316,6 +2345,22 @@ static av_always_inline void set_ct_depth(const HEVCSPS *sps, uint8_t *tab_ct_de
                ct_depth, length);
 }
 
+/* VideoEye: persist the CU's part_mode per min-CB so it can be exported with
+ * the block-info side data (the decoder otherwise keeps it only transiently). */
+static av_always_inline void set_part_mode(const HEVCSPS *sps, uint8_t *tab_part_mode,
+                                           int x0, int y0,
+                                           int log2_cb_size, int part_mode)
+{
+    int length = (1 << log2_cb_size) >> sps->log2_min_cb_size;
+    int x_cb   = x0 >> sps->log2_min_cb_size;
+    int y_cb   = y0 >> sps->log2_min_cb_size;
+    int y;
+
+    for (y = 0; y < length; y++)
+        memset(&tab_part_mode[(y_cb + y) * sps->min_cb_width + x_cb],
+               part_mode, length);
+}
+
 static const uint8_t tab_mode_idx[] = {
      0,  1,  2,  2,  2,  2,  3,  5,  7,  8, 10, 12, 13, 15, 17, 18, 19, 20,
     21, 22, 23, 23, 24, 24, 25, 25, 26, 27, 27, 28, 28, 29, 29, 30, 31};
@@ -2415,6 +2460,17 @@ static void intra_prediction_unit_default_value(HEVCLocalContext *lc,
                 tab_mvf[(y_pu + j) * min_pu_width + x_pu + k].pred_flag = PF_INTRA;
 }
 
+/* VideoEye: exact CABAC stream bit position. The decoder reads whole bytes
+ * (2 at a time) into cc.low but consumes them bit-by-bit via renormalization;
+ * cc.low's low 16 bits hold the not-yet-consumed buffer with a marker at its
+ * lowest set bit, so 16 - ctz(low & 0xFFFF) bits are still buffered. */
+static av_always_inline int veye_cabac_bits(const CABACContext *c)
+{
+    int lo = c->low & 0xFFFF;
+    int buffered = lo ? (16 - ff_ctz(lo)) : 0;
+    return (int)(c->bytestream - c->bytestream_start) * 8 - buffered;
+}
+
 static int hls_coding_unit(HEVCLocalContext *lc, const HEVCContext *s,
                            const HEVCLayerContext *l,
                            const HEVCPPS *pps, const HEVCSPS *sps,
@@ -2436,6 +2492,14 @@ static int hls_coding_unit(HEVCLocalContext *lc, const HEVCContext *s,
     lc->cu.part_mode        = PART_2Nx2N;
     lc->cu.intra_split_flag = 0;
 
+    /* VideoEye: exact CABAC bit position at CU start / after prediction syntax,
+     * to derive this CU's coded bit cost (total / prediction / residual). The
+     * arithmetic decoder buffers up-to-16 look-ahead bits in cc.low; subtract
+     * them from the bytestream byte position so deltas are bit-exact (the raw
+     * byte position only advances in 2-byte refills). */
+    int veye_bits_start = veye_cabac_bits(&lc->cc);
+    int veye_bits_pred_end = veye_bits_start;    /* after prediction units */
+
     SAMPLE_CTB(l->skip_flag, x_cb, y_cb) = 0;
     for (x = 0; x < 4; x++)
         lc->pu.intra_pred_mode[x] = 1;
@@ -2471,6 +2535,7 @@ static int hls_coding_unit(HEVCLocalContext *lc, const HEVCContext *s,
         hls_prediction_unit(lc, l, pps, sps,
                             x0, y0, cb_size, cb_size, log2_cb_size, 0, idx);
         intra_prediction_unit_default_value(lc, l, sps, x0, y0, log2_cb_size);
+        veye_bits_pred_end = veye_cabac_bits(&lc->cc);  // skip CU: no residual
 
         if (!s->sh.disable_deblocking_filter_flag)
             ff_hevc_deblocking_boundary_strengths(lc, l, pps, x0, y0, log2_cb_size);
@@ -2559,6 +2624,8 @@ static int hls_coding_unit(HEVCLocalContext *lc, const HEVCContext *s,
             }
         }
 
+        veye_bits_pred_end = veye_cabac_bits(&lc->cc);  // prediction ends here
+
         if (!pcm_flag) {
             int rqt_root_cbf = 1;
 
@@ -2583,6 +2650,20 @@ static int hls_coding_unit(HEVCLocalContext *lc, const HEVCContext *s,
         }
     }
 
+    /* VideoEye: record this CU's coded bit cost at its top-left min-CB. The
+     * CABAC bytestream advances as symbols are read, so position deltas give an
+     * (byte-resolution) approximation of bits spent on prediction vs residual. */
+    {
+        int bend  = veye_cabac_bits(&lc->cc);
+        int total = bend - veye_bits_start;             /* whole CU (exact) */
+        int tu    = bend - veye_bits_pred_end;          /* residual (exact) */
+        int pu    = veye_bits_pred_end - veye_bits_start;  /* prediction + hdr */
+        int cb_idx = y_cb * min_cb_width + x_cb;
+        l->tab_cu_bits[cb_idx] = total < 0 ? 0 : total;
+        l->tab_pu_bits[cb_idx] = pu < 0 ? 0 : pu;
+        l->tab_tu_bits[cb_idx] = tu < 0 ? 0 : tu;
+    }
+
     if (pps->cu_qp_delta_enabled_flag && lc->tu.is_cu_qp_delta_coded == 0)
         ff_hevc_set_qPy(lc, l, pps, x0, y0, log2_cb_size);
 
@@ -2598,6 +2679,7 @@ static int hls_coding_unit(HEVCLocalContext *lc, const HEVCContext *s,
     }
 
     set_ct_depth(sps, l->tab_ct_depth, x0, y0, log2_cb_size, lc->ct_depth);
+    set_part_mode(sps, l->tab_part_mode, x0, y0, log2_cb_size, lc->cu.part_mode);
 
     return 0;
 }
@@ -3281,6 +3363,7 @@ static int hevc_frame_start(HEVCContext *s, HEVCLayerContext *l,
     memset(l->horizontal_bs, 0, l->bs_width * l->bs_height);
     memset(l->vertical_bs,   0, l->bs_width * l->bs_height);
     memset(l->cbf_luma,      0, sps->min_tb_width * sps->min_tb_height);
+    memset(l->tab_tu_log2,   0, sps->min_tb_width * sps->min_tb_height);
     memset(l->is_pcm,        0, (sps->min_pu_width + 1) * (sps->min_pu_height + 1));
     memset(l->tab_slice_address, -1, pic_size_in_ctb * sizeof(*l->tab_slice_address));
 
@@ -3387,8 +3470,14 @@ static int hevc_frame_start(HEVCContext *s, HEVCLayerContext *l,
     if (!s->avctx->hwaccel) {
         AVFrame *vf = s->cur_frame->needs_fg ? s->cur_frame->frame_grain
                                              : s->cur_frame->f;
+        const HEVCPPS *pps = s->pps;
+        int ntc = pps ? pps->num_tile_columns : 1;
+        int ntr = pps ? pps->num_tile_rows : 1;
         ff_veye_alloc_blockinfo_hevc(vf, sps->min_cb_width, sps->min_cb_height,
-                                     sps->log2_min_cb_size);
+                                     sps->log2_min_cb_size,
+                                     sps->min_tb_width, sps->min_tb_height,
+                                     sps->log2_min_tb_size,
+                                     sps->ctb_width, sps->ctb_height, ntc, ntr);
     }
 
     ret = ff_hevc_output_frames(s, s->layers_active_decode, s->layers_active_output,
@@ -3546,13 +3635,36 @@ static int hevc_frame_end(HEVCContext *s, HEVCLayerContext *l)
     if (l->sps && !s->avctx->hwaccel) {
         const HEVCSPS *sps = l->sps;
         AVFrame *vf = out->needs_fg ? out->frame_grain : out->f;
-        const int8_t *pf = out->tab_mvf
-            ? (const int8_t *)&out->tab_mvf[0].pred_flag : NULL;
+        static const VeyeMvFieldLayout mvf_layout = {
+            offsetof(MvField, mv[0]), offsetof(MvField, mv[1]),
+            offsetof(MvField, ref_idx), offsetof(MvField, pred_flag),
+        };
+        /* This frame's reference picture POCs (last slice's L0/L1 lists). */
+        int32_t l0_poc[VEYE_MAX_REFS] = {0}, l1_poc[VEYE_MAX_REFS] = {0};
+        int nb_l0 = 0, nb_l1 = 0, i;
+        if (out->refPicList) {
+            const RefPicList *r0 = &out->refPicList[0];
+            const RefPicList *r1 = &out->refPicList[1];
+            nb_l0 = r0->nb_refs < VEYE_MAX_REFS ? r0->nb_refs : VEYE_MAX_REFS;
+            nb_l1 = r1->nb_refs < VEYE_MAX_REFS ? r1->nb_refs : VEYE_MAX_REFS;
+            for (i = 0; i < nb_l0; i++) l0_poc[i] = r0->list[i];
+            for (i = 0; i < nb_l1; i++) l1_poc[i] = r1->list[i];
+        }
+        const HEVCPPS *pps = s->pps;
+        int ntc = pps ? pps->num_tile_columns : 1;
+        int ntr = pps ? pps->num_tile_rows : 1;
         ff_veye_export_blockinfo_hevc(vf,
-            l->skip_flag, l->tab_ct_depth, l->qp_y_tab,
-            l->tab_ipm, l->is_pcm, pf, sizeof(MvField),
+            l->skip_flag, l->tab_ct_depth, l->tab_part_mode, l->qp_y_tab,
+            l->tab_ipm, l->is_pcm, out->tab_mvf, sizeof(MvField), &mvf_layout,
+            l->tab_tu_log2,
+            out->poc, l0_poc, nb_l0, l1_poc, nb_l1,
+            l->tab_slice_address, sps->ctb_width, sps->ctb_height, ntc, ntr,
+            pps ? pps->col_bd : NULL, pps ? pps->row_bd : NULL,
+            l->tab_cu_bits, l->tab_pu_bits, l->tab_tu_bits,
             sps->min_cb_width, sps->min_cb_height, sps->min_pu_width,
-            sps->log2_min_cb_size, sps->log2_ctb_size, sps->log2_min_pu_size);
+            sps->min_tb_width, sps->min_tb_height,
+            sps->log2_min_cb_size, sps->log2_ctb_size, sps->log2_min_pu_size,
+            sps->log2_min_tb_size);
     }
 
     return 0;
diff --git a/libavcodec/hevc/hevcdec.h b/libavcodec/hevc/hevcdec.h
index 911d5c3..8df29c6 100644
--- a/libavcodec/hevc/hevcdec.h
+++ b/libavcodec/hevc/hevcdec.h
@@ -466,11 +466,19 @@ typedef struct HEVCLayerContext {
     //  CU
     uint8_t                *skip_flag;
     uint8_t                *tab_ct_depth;
+    uint8_t                *tab_part_mode; // VideoEye: PU part_mode per min-CB
+    // VideoEye: coded bit cost per CU, stored at the CU's top-left min-CB.
+    // tab_cu_bits = total; tab_pu_bits = prediction syntax; tab_tu_bits =
+    // residual/transform. Derived from CABAC bytestream position deltas.
+    int32_t                *tab_cu_bits;
+    int32_t                *tab_pu_bits;
+    int32_t                *tab_tu_bits;
 
     // PU
     uint8_t                *cbf_luma; // cbf_luma of colocated TU
     uint8_t                *tab_ipm;
     uint8_t                *is_pcm;
+    uint8_t                *tab_tu_log2; // VideoEye: luma TU log2 size per min-TB
 
     // CTB-level flags affecting loop filter operation
     uint8_t                *filter_slice_edges;
diff --git a/libavcodec/veye_blockinfo.c b/libavcodec/veye_blockinfo.c
index 2d817c7..667f9ba 100644
--- a/libavcodec/veye_blockinfo.c
+++ b/libavcodec/veye_blockinfo.c
@@ -7,25 +7,51 @@
 
 #include "libavutil/error.h"
 #include "libavutil/frame.h"
+#include "libavutil/mem.h"
+
+#include "libavcodec/codec_id.h"
 
 #include "veye_blockinfo.h"
+#include "veye_aom_inspect.h"
 
 int ff_veye_export_blockinfo(AVFrame *frame, enum AVCodecID codec_id,
                              const uint32_t *mb_type, const int8_t *qscale,
+                             const int32_t *mb_bits,
+                             int16_t (*const motion_val[2])[2],
+                             int8_t *const ref_index[2],
+                             int own_poc,
+                             const int32_t *l0_poc, int nb_l0,
+                             const int32_t *l1_poc, int nb_l1,
                              int grid_w, int grid_h, int grid_stride,
                              int block_unit)
 {
     VeyeBlockInfoHeader *hdr;
     VeyeBlockRecord *rec;
+    VeyeRefHeader *rh;
+    int32_t *bits;
     AVFrameSideData *sd;
     size_t n, size;
-    int x, y;
+    int x, y, i;
 
     if (!frame || !mb_type || grid_w <= 0 || grid_h <= 0)
         return 0;
 
     n    = (size_t)grid_w * (size_t)grid_h;
-    size = sizeof(VeyeBlockInfoHeader) + n * sizeof(VeyeBlockRecord);
+    /* v5: a fixed-size reference section is appended after the records, mirroring
+     * the HEVC layout, so the consumer can mark references on the frame chart.
+     * v8: followed by 3*n int32 per-MB bit costs (total, prediction, transform);
+     * raster order, all zero when mb_bits is NULL.
+     * v9: followed by an aux section -- n int8 intra type, n int8 luma intra
+     * mode, n int16 slice id. mb_bits is read with stride VEYE_H264_MB_STRIDE.
+     * v10: followed by an MV section -- per MB, 4 8x8 quadrants each carrying
+     * mv0_x,mv0_y,mv1_x,mv1_y (int16, 1/4-pel) then ref0,ref1 (int8).
+     * v11: followed by n*16 int8 -- canonical luma intra mode of each 4x4 block
+     * (H.264 block scan order; -1 if not intra). */
+    size = sizeof(VeyeBlockInfoHeader) + n * sizeof(VeyeBlockRecord)
+           + sizeof(VeyeRefHeader) + 3 * n * sizeof(int32_t)
+           + n * (1 + 1 + 2)
+           + n * 4 * (4 * sizeof(int16_t) + 2 * sizeof(int8_t))
+           + n * 16 * sizeof(int8_t);
 
     sd = av_frame_new_side_data(frame, AV_FRAME_DATA_VEYE_BLOCKINFO, size);
     if (!sd)
@@ -51,6 +77,101 @@ int ff_veye_export_blockinfo(AVFrame *frame, enum AVCodecID codec_id,
         }
     }
 
+    rh = (VeyeRefHeader *)(sd->data + sizeof(VeyeBlockInfoHeader)
+                           + n * sizeof(VeyeBlockRecord));
+    if (nb_l0 > VEYE_MAX_REFS) nb_l0 = VEYE_MAX_REFS;
+    if (nb_l1 > VEYE_MAX_REFS) nb_l1 = VEYE_MAX_REFS;
+    if (nb_l0 < 0) nb_l0 = 0;
+    if (nb_l1 < 0) nb_l1 = 0;
+    rh->own_poc = own_poc;
+    rh->nb_l0   = nb_l0;
+    rh->nb_l1   = nb_l1;
+    for (i = 0; i < VEYE_MAX_REFS; i++) {
+        rh->l0_poc[i] = (l0_poc && i < nb_l0) ? l0_poc[i] : 0;
+        rh->l1_poc[i] = (l1_poc && i < nb_l1) ? l1_poc[i] : 0;
+    }
+
+    /* v8 bit section + v9 aux section. mb_bits is stored [xy*STRIDE + k] with
+     * xy = x + y*grid_stride (same layout as mb_type), STRIDE elements per MB:
+     * 0..2 total/pred/transform bits, 3 intra type, 4 luma mode, 5 slice id. */
+    bits = (int32_t *)(sd->data + sizeof(VeyeBlockInfoHeader)
+                       + n * sizeof(VeyeBlockRecord) + sizeof(VeyeRefHeader));
+    {
+        int8_t  *aux_type  = (int8_t *)(bits + 3 * n);
+        int8_t  *aux_mode  = aux_type + n;
+        int16_t *aux_slice = (int16_t *)(aux_mode + n);
+        if (mb_bits) {
+            const int S = VEYE_H264_MB_STRIDE;
+            for (y = 0; y < grid_h; y++)
+                for (x = 0; x < grid_w; x++) {
+                    int xy = x + y * grid_stride;
+                    int o  = x + y * grid_w;
+                    bits[o * 3 + 0] = mb_bits[xy * S + 0];
+                    bits[o * 3 + 1] = mb_bits[xy * S + 1];
+                    bits[o * 3 + 2] = mb_bits[xy * S + 2];
+                    aux_type[o]  = (int8_t)mb_bits[xy * S + 3];
+                    aux_mode[o]  = (int8_t)mb_bits[xy * S + 4];
+                    aux_slice[o] = (int16_t)mb_bits[xy * S + 5];
+                }
+        } else {
+            memset(bits, 0, 3 * n * sizeof(int32_t));
+            memset(aux_type, 0, n * (1 + 1 + 2));
+        }
+
+        /* v10 MV section: 4 quadrants/MB, each (mv0,mv1) int16 + (ref0,ref1)
+         * int8. Quadrant q's representative 4x4 block (h264 motion_val is
+         * per-4x4, 1/4-pel; mv_stride = grid_w*4). */
+        {
+            int16_t *mv = (int16_t *)(aux_slice + n);
+            int8_t  *rf = (int8_t *)(mv + n * 4 * 4);
+            int mv_stride = grid_w * 4;
+            memset(mv, 0, n * 4 * 4 * sizeof(int16_t));
+            memset(rf, 0, n * 4 * 2 * sizeof(int8_t));
+            for (y = 0; y < grid_h; y++)
+                for (x = 0; x < grid_w; x++) {
+                    int xy = x + y * grid_stride;
+                    int o  = x + y * grid_w;
+                    int q;
+                    for (q = 0; q < 4; q++) {
+                        int qx = q & 1, qy = q >> 1;
+                        int bxy = (4 * x + 2 * qx) + (4 * y + 2 * qy) * mv_stride;
+                        int m = (o * 4 + q) * 4;
+                        int rfi = (o * 4 + q) * 2;
+                        if (motion_val && motion_val[0]) {
+                            mv[m + 0] = motion_val[0][bxy][0];
+                            mv[m + 1] = motion_val[0][bxy][1];
+                        }
+                        if (motion_val && motion_val[1]) {
+                            mv[m + 2] = motion_val[1][bxy][0];
+                            mv[m + 3] = motion_val[1][bxy][1];
+                        }
+                        rf[rfi + 0] = (ref_index && ref_index[0])
+                                      ? ref_index[0][4 * xy + q] : -1;
+                        rf[rfi + 1] = (ref_index && ref_index[1])
+                                      ? ref_index[1][4 * xy + q] : -1;
+                    }
+                }
+
+            /* v11: per-4x4 luma intra modes (16/MB), scan order. */
+            {
+                int8_t *modes = (int8_t *)(rf + n * 4 * 2);
+                int k;
+                if (mb_bits) {
+                    const int S = VEYE_H264_MB_STRIDE;
+                    for (y = 0; y < grid_h; y++)
+                        for (x = 0; x < grid_w; x++) {
+                            int xy = x + y * grid_stride;
+                            int o  = x + y * grid_w;
+                            for (k = 0; k < 16; k++)
+                                modes[o * 16 + k] = (int8_t)mb_bits[xy * S + 6 + k];
+                        }
+                } else {
+                    memset(modes, -1, n * 16);
+                }
+            }
+        }
+    }
+
     return 0;
 }
 
@@ -62,16 +183,43 @@ int ff_veye_export_blockinfo(AVFrame *frame, enum AVCodecID codec_id,
 
 /* Allocate the HEVC side data on @frame (or return the one already attached)
  * and (re)write its header. On a fresh allocation the records are zeroed. */
+/* Total side-data size: header + min-CB records + TU sub-header + min-TB grid
+ * + reference section + picture-structure section (slice id grid + tile bds). */
+static size_t veye_hevc_size(int min_cb_width, int min_cb_height,
+                             int min_tb_width, int min_tb_height,
+                             int ctb_width, int ctb_height,
+                             int num_tile_cols, int num_tile_rows)
+{
+    size_t n_cb = (size_t)min_cb_width * (size_t)min_cb_height;
+    size_t n_tb = (size_t)min_tb_width * (size_t)min_tb_height;
+    size_t n_ctb = (size_t)ctb_width * (size_t)ctb_height;
+    size_t struct_sz = sizeof(VeyeStructHeader)
+                       + n_ctb * sizeof(int32_t)
+                       + (size_t)(num_tile_cols + 1) * sizeof(uint32_t)
+                       + (size_t)(num_tile_rows + 1) * sizeof(uint32_t);
+    size_t bits_sz = 3 * n_cb * sizeof(int32_t);  // cu/pu/tu bit-cost grids
+    return sizeof(VeyeBlockInfoHeader) + n_cb * sizeof(VeyeBlockRecordHEVC)
+           + sizeof(VeyeTuHeader) + n_tb + sizeof(VeyeRefHeader)
+           + struct_sz + bits_sz;
+}
+
 static AVFrameSideData *veye_hevc_side_data(AVFrame *frame, int min_cb_width,
                                             int min_cb_height,
-                                            int log2_min_cb_size)
+                                            int log2_min_cb_size,
+                                            int min_tb_width, int min_tb_height,
+                                            int log2_min_tb_size,
+                                            int ctb_width, int ctb_height,
+                                            int num_tile_cols, int num_tile_rows)
 {
     VeyeBlockInfoHeader *hdr;
+    VeyeTuHeader *tuh;
     AVFrameSideData *sd;
-    size_t n, size;
+    size_t n_cb, size;
 
-    n    = (size_t)min_cb_width * (size_t)min_cb_height;
-    size = sizeof(VeyeBlockInfoHeader) + n * sizeof(VeyeBlockRecordHEVC);
+    n_cb = (size_t)min_cb_width * (size_t)min_cb_height;
+    size = veye_hevc_size(min_cb_width, min_cb_height,
+                          min_tb_width, min_tb_height,
+                          ctb_width, ctb_height, num_tile_cols, num_tile_rows);
 
     sd = av_frame_get_side_data(frame, AV_FRAME_DATA_VEYE_BLOCKINFO);
     if (sd && sd->size == size) {
@@ -94,31 +242,59 @@ static AVFrameSideData *veye_hevc_side_data(AVFrame *frame, int min_cb_width,
     hdr->grid_w      = (uint32_t)min_cb_width;
     hdr->grid_h      = (uint32_t)min_cb_height;
     hdr->block_unit  = (uint32_t)(1 << log2_min_cb_size);
-    hdr->n_records   = (uint32_t)n;
+    hdr->n_records   = (uint32_t)n_cb;
+
+    tuh = (VeyeTuHeader *)(sd->data + sizeof(VeyeBlockInfoHeader)
+                           + n_cb * sizeof(VeyeBlockRecordHEVC));
+    tuh->tu_grid_w = (uint32_t)min_tb_width;
+    tuh->tu_grid_h = (uint32_t)min_tb_height;
+    tuh->tu_unit   = (uint32_t)(1 << log2_min_tb_size);
     return sd;
 }
 
 int ff_veye_alloc_blockinfo_hevc(AVFrame *frame, int min_cb_width,
-                                 int min_cb_height, int log2_min_cb_size)
+                                 int min_cb_height, int log2_min_cb_size,
+                                 int min_tb_width, int min_tb_height,
+                                 int log2_min_tb_size,
+                                 int ctb_width, int ctb_height,
+                                 int num_tile_cols, int num_tile_rows)
 {
     if (!frame || min_cb_width <= 0 || min_cb_height <= 0)
         return 0;
     return veye_hevc_side_data(frame, min_cb_width, min_cb_height,
-                               log2_min_cb_size) ? 0 : AVERROR(ENOMEM);
+                               log2_min_cb_size, min_tb_width, min_tb_height,
+                               log2_min_tb_size, ctb_width, ctb_height,
+                               num_tile_cols, num_tile_rows)
+           ? 0 : AVERROR(ENOMEM);
 }
 
 int ff_veye_export_blockinfo_hevc(AVFrame *frame,
                                   const uint8_t *skip_flag,
                                   const uint8_t *tab_ct_depth,
+                                  const uint8_t *tab_part_mode,
                                   const int8_t  *qp_y_tab,
                                   const uint8_t *tab_ipm,
                                   const uint8_t *is_pcm,
-                                  const int8_t  *mvf_pred_flag,
+                                  const void   *tab_mvf,
                                   size_t mvf_elem_size,
+                                  const VeyeMvFieldLayout *mvf_layout,
+                                  const uint8_t *tab_tu_log2,
+                                  int own_poc,
+                                  const int32_t *l0_poc, int nb_l0,
+                                  const int32_t *l1_poc, int nb_l1,
+                                  const int32_t *tab_slice_address,
+                                  int ctb_width, int ctb_height,
+                                  int num_tile_cols, int num_tile_rows,
+                                  const unsigned int *col_bd,
+                                  const unsigned int *row_bd,
+                                  const int32_t *tab_cu_bits,
+                                  const int32_t *tab_pu_bits,
+                                  const int32_t *tab_tu_bits,
                                   int min_cb_width, int min_cb_height,
                                   int min_pu_width,
+                                  int min_tb_width, int min_tb_height,
                                   int log2_min_cb_size, int log2_ctb_size,
-                                  int log2_min_pu_size)
+                                  int log2_min_pu_size, int log2_min_tb_size)
 {
     VeyeBlockRecordHEVC *rec;
     AVFrameSideData *sd;
@@ -129,7 +305,9 @@ int ff_veye_export_blockinfo_hevc(AVFrame *frame,
         return 0;
 
     sd = veye_hevc_side_data(frame, min_cb_width, min_cb_height,
-                             log2_min_cb_size);
+                             log2_min_cb_size, min_tb_width, min_tb_height,
+                             log2_min_tb_size, ctb_width, ctb_height,
+                             num_tile_cols, num_tile_rows);
     if (!sd)
         return AVERROR(ENOMEM);
 
@@ -142,31 +320,275 @@ int ff_veye_export_blockinfo_hevc(AVFrame *frame,
             int y_pu  = y << cb_to_pu;
             int pu    = x_pu + y_pu * min_pu_width;
             int cu_log2 = log2_ctb_size - depth;
+            const char *mvf = NULL;
+            int8_t pf = 0;
             int pred;
 
             if (cu_log2 < log2_min_cb_size) cu_log2 = log2_min_cb_size;
             if (cu_log2 > log2_ctb_size)    cu_log2 = log2_ctb_size;
 
+            if (tab_mvf && mvf_layout) {
+                mvf = (const char *)tab_mvf + (size_t)pu * mvf_elem_size;
+                pf  = *(const int8_t *)(mvf + mvf_layout->flag_off);
+            }
+
             if (is_pcm && is_pcm[pu]) {
                 pred = VEYE_PRED_IPCM;
             } else if (skip_flag && skip_flag[cb]) {
                 pred = VEYE_PRED_SKIP;
             } else {
-                int8_t pf = mvf_pred_flag
-                    ? *(const int8_t *)((const char *)mvf_pred_flag
-                                        + (size_t)pu * mvf_elem_size)
-                    : 0;
                 pred = (pf == 0) ? VEYE_PRED_INTRA : VEYE_PRED_INTER;
             }
 
             rec->cu_log2    = (uint8_t)cu_log2;
             rec->pred       = (uint8_t)pred;
             rec->intra_mode = tab_ipm ? tab_ipm[pu] : 0;
-            rec->reserved   = 0;
+            rec->pred_flag  = (uint8_t)pf;
             rec->qp         = qp_y_tab ? qp_y_tab[cb] : -1;
+
+            rec->mv0_x = rec->mv0_y = rec->mv1_x = rec->mv1_y = 0;
+            rec->ref0  = rec->ref1  = -1;
+            if (mvf) {
+                const int8_t *ref = (const int8_t *)(mvf + mvf_layout->ref_off);
+                if (pf & 1) {  /* PF_L0 */
+                    const int16_t *m =
+                        (const int16_t *)(mvf + mvf_layout->mv0_off);
+                    rec->mv0_x = m[0];
+                    rec->mv0_y = m[1];
+                    rec->ref0  = ref[0];
+                }
+                if (pf & 2) {  /* PF_L1 */
+                    const int16_t *m =
+                        (const int16_t *)(mvf + mvf_layout->mv1_off);
+                    rec->mv1_x = m[0];
+                    rec->mv1_y = m[1];
+                    rec->ref1  = ref[1];
+                }
+            }
+            rec->part_mode = tab_part_mode ? tab_part_mode[cb] : 0;
+            rec->ct_depth  = (uint8_t)depth;  /* CU coding-tree depth (0=CTB) */
+            rec++;
+        }
+    }
+
+    /* Append the luma TU-size grid (min-TB granularity), after the records
+     * and the VeyeTuHeader that veye_hevc_side_data already wrote. */
+    {
+        size_t n_cb = (size_t)min_cb_width * (size_t)min_cb_height;
+        size_t n_tb = (size_t)min_tb_width * (size_t)min_tb_height;
+        size_t base = sizeof(VeyeBlockInfoHeader)
+                    + n_cb * sizeof(VeyeBlockRecordHEVC)
+                    + sizeof(VeyeTuHeader);
+        if (tab_tu_log2 && min_tb_width > 0 && min_tb_height > 0)
+            memcpy(sd->data + base, tab_tu_log2, n_tb);
+
+        /* Reference section, right after the TU grid. */
+        VeyeRefHeader *rh = (VeyeRefHeader *)(sd->data + base + n_tb);
+        int i;
+        rh->own_poc = own_poc;
+        rh->nb_l0 = nb_l0 < VEYE_MAX_REFS ? nb_l0 : VEYE_MAX_REFS;
+        rh->nb_l1 = nb_l1 < VEYE_MAX_REFS ? nb_l1 : VEYE_MAX_REFS;
+        for (i = 0; i < rh->nb_l0; i++)
+            rh->l0_poc[i] = l0_poc ? l0_poc[i] : 0;
+        for (i = 0; i < rh->nb_l1; i++)
+            rh->l1_poc[i] = l1_poc ? l1_poc[i] : 0;
+
+        /* Picture-structure section (v6): VeyeStructHeader + per-CTB slice id
+         * grid + tile column/row boundaries (in pixels), after the ref section. */
+        {
+            size_t soff = base + n_tb + sizeof(VeyeRefHeader);
+            VeyeStructHeader *sh = (VeyeStructHeader *)(sd->data + soff);
+            size_t n_ctb = (size_t)ctb_width * (size_t)ctb_height;
+            int ctb_size = 1 << log2_ctb_size;
+            int32_t *slice_id;
+            uint32_t *cbd, *rbd;
+            int t;
+
+            sh->ctb_size      = (uint32_t)ctb_size;
+            sh->ctb_width     = (uint32_t)ctb_width;
+            sh->ctb_height    = (uint32_t)ctb_height;
+            sh->num_tile_cols = (uint32_t)num_tile_cols;
+            sh->num_tile_rows = (uint32_t)num_tile_rows;
+
+            slice_id = (int32_t *)(sd->data + soff + sizeof(VeyeStructHeader));
+            if (tab_slice_address)
+                memcpy(slice_id, tab_slice_address, n_ctb * sizeof(int32_t));
+            else
+                memset(slice_id, 0, n_ctb * sizeof(int32_t));
+
+            cbd = (uint32_t *)(slice_id + n_ctb);
+            rbd = cbd + (num_tile_cols + 1);
+            for (t = 0; t <= num_tile_cols; t++)
+                cbd[t] = (uint32_t)((col_bd ? col_bd[t] : 0) * ctb_size);
+            for (t = 0; t <= num_tile_rows; t++)
+                rbd[t] = (uint32_t)((row_bd ? row_bd[t] : 0) * ctb_size);
+
+            /* Bit-cost section (v7): cu/pu/tu bit grids (min-CB raster), after
+             * the tile boundaries. Values are stored at CU top-left cells. */
+            {
+                size_t boff = soff + sizeof(VeyeStructHeader)
+                            + n_ctb * sizeof(int32_t)
+                            + (size_t)(num_tile_cols + 1) * sizeof(uint32_t)
+                            + (size_t)(num_tile_rows + 1) * sizeof(uint32_t);
+                int32_t *cu_b = (int32_t *)(sd->data + boff);
+                int32_t *pu_b = cu_b + n_cb;
+                int32_t *tu_b = pu_b + n_cb;
+                if (tab_cu_bits) memcpy(cu_b, tab_cu_bits, n_cb * sizeof(int32_t));
+                else memset(cu_b, 0, n_cb * sizeof(int32_t));
+                if (tab_pu_bits) memcpy(pu_b, tab_pu_bits, n_cb * sizeof(int32_t));
+                else memset(pu_b, 0, n_cb * sizeof(int32_t));
+                if (tab_tu_bits) memcpy(tu_b, tab_tu_bits, n_cb * sizeof(int32_t));
+                else memset(tu_b, 0, n_cb * sizeof(int32_t));
+            }
+        }
+    }
+
+    return 0;
+}
+
+/* libaom: ref_frame[0] == INTRA_FRAME (0) marks an intra block; >= 1 is inter.
+ * NONE_FRAME is -1. */
+#define VEYE_AV1_INTRA_FRAME 0
+
+int ff_veye_serialize_blockinfo_av1(const struct veye_insp_frame_data *fd,
+                                    uint8_t **out_buf, size_t *out_size)
+{
+    VeyeBlockInfoHeader *hdr;
+    VeyeBlockRecordAV1 *rec;
+    VeyeRefHeader *rh;
+    uint8_t *buf;
+    size_t n, size;
+    int x, y, r, i;
+    uint8_t slot_used[7] = {0};  /* which reference slots blocks actually use */
+
+    *out_buf = NULL;
+    *out_size = 0;
+    if (!fd || !fd->mi_grid || fd->mi_cols <= 0 || fd->mi_rows <= 0)
+        return 0;
+
+    n    = (size_t)fd->mi_cols * (size_t)fd->mi_rows;
+    /* v5: a reference section (own + referenced display orders) is appended
+     * after the records, mirroring the H.264/HEVC layout. */
+    size = sizeof(VeyeBlockInfoHeader) + n * sizeof(VeyeBlockRecordAV1)
+           + sizeof(VeyeRefHeader);
+
+    buf = av_malloc(size);
+    if (!buf)
+        return AVERROR(ENOMEM);
+
+    hdr = (VeyeBlockInfoHeader *)buf;
+    hdr->magic       = VEYE_BI_MAGIC;
+    hdr->version     = VEYE_BI_VERSION;
+    hdr->record_size = (uint16_t)sizeof(VeyeBlockRecordAV1);
+    hdr->codec_id    = (uint32_t)AV_CODEC_ID_AV1;
+    hdr->grid_w      = (uint32_t)fd->mi_cols;
+    hdr->grid_h      = (uint32_t)fd->mi_rows;
+    hdr->block_unit  = 4;  /* MI_SIZE: each cell is a 4x4 luma block */
+    hdr->n_records   = (uint32_t)n;
+
+    rec = (VeyeBlockRecordAV1 *)(buf + sizeof(VeyeBlockInfoHeader));
+    for (y = 0; y < fd->mi_rows; y++) {
+        for (x = 0; x < fd->mi_cols; x++) {
+            const veye_insp_mi_data *mi = &fd->mi_grid[y * fd->mi_cols + x];
+            int inter = mi->ref_frame[0] > VEYE_AV1_INTRA_FRAME;
+
+            rec->bsize   = (uint8_t)mi->bsize;
+            rec->pred    = (uint8_t)(inter ? VEYE_PRED_INTER : VEYE_PRED_INTRA);
+            rec->mode    = (uint8_t)mi->mode;
+            rec->skip    = (uint8_t)mi->skip;
+            rec->qp      = mi->current_qindex;
+            rec->mv0_x   = mi->mv[0].col;
+            rec->mv0_y   = mi->mv[0].row;
+            rec->mv1_x   = mi->mv[1].col;
+            rec->mv1_y   = mi->mv[1].row;
+            rec->ref0    = (int8_t)mi->ref_frame[0];
+            rec->ref1    = (int8_t)mi->ref_frame[1];
+            rec->tx_size = (uint8_t)mi->tx_size;
+            rec->palette = (uint8_t)(mi->palette > 0 ? mi->palette : 0);
+            /* Note which reference slots (LAST_FRAME=1..ALTREF_FRAME=7) are
+             * actually referenced, so we export only used references. */
+            for (i = 0; i < 2; i++) {
+                int rf = mi->ref_frame[i];
+                if (rf >= 1 && rf <= 7)
+                    slot_used[rf - 1] = 1;
+            }
             rec++;
         }
     }
 
+    /* Reference section: own display order, plus used references split into
+     * past (L0) and future (L1) by display order, de-duplicated. AV1 has no
+     * L0/L1 lists, so this split is purely for the consumer's two-color
+     * frame-chart markers. */
+    rh = (VeyeRefHeader *)(buf + sizeof(VeyeBlockInfoHeader)
+                           + n * sizeof(VeyeBlockRecordAV1));
+    rh->own_poc = fd->order_hint;
+    rh->nb_l0 = 0;
+    rh->nb_l1 = 0;
+    for (i = 0; i < VEYE_MAX_REFS; i++)
+        rh->l0_poc[i] = rh->l1_poc[i] = 0;
+    for (r = 0; r < 7; r++) {
+        int oh, dup, j;
+        if (!slot_used[r] || fd->ref_frame_order_hint[r] < 0)
+            continue;
+        oh = fd->ref_frame_order_hint[r];
+        if (oh == fd->order_hint)
+            continue;  /* self / not a real reference */
+        if (oh < fd->order_hint) {        /* past reference -> L0 */
+            for (dup = 0, j = 0; j < rh->nb_l0; j++)
+                if (rh->l0_poc[j] == oh) { dup = 1; break; }
+            if (!dup && rh->nb_l0 < VEYE_MAX_REFS)
+                rh->l0_poc[rh->nb_l0++] = oh;
+        } else {                          /* future reference -> L1 */
+            for (dup = 0, j = 0; j < rh->nb_l1; j++)
+                if (rh->l1_poc[j] == oh) { dup = 1; break; }
+            if (!dup && rh->nb_l1 < VEYE_MAX_REFS)
+                rh->l1_poc[rh->nb_l1++] = oh;
+        }
+    }
+
+    *out_buf = buf;
+    *out_size = size;
+    return 0;
+}
+
+int ff_veye_export_blockinfo_av1(AVFrame *frame,
+                                 const struct veye_insp_frame_data *fd)
+{
+    uint8_t *buf = NULL;
+    size_t size = 0;
+    AVFrameSideData *sd;
+    int ret;
+
+    if (!frame)
+        return 0;
+    ret = ff_veye_serialize_blockinfo_av1(fd, &buf, &size);
+    if (ret < 0)
+        return ret;
+    if (!buf)
+        return 0;
+
+    sd = av_frame_new_side_data(frame, AV_FRAME_DATA_VEYE_BLOCKINFO, size);
+    if (!sd) {
+        av_free(buf);
+        return AVERROR(ENOMEM);
+    }
+    memcpy(sd->data, buf, size);
+    av_free(buf);
+    return 0;
+}
+
+/* Attach a pre-serialized AV1 payload (from ff_veye_serialize_blockinfo_av1)
+ * to a frame. Used by the decoder to attach the correct frame's buffered
+ * block info when output order differs from decode order. */
+int ff_veye_attach_blockinfo(AVFrame *frame, const uint8_t *buf, size_t size)
+{
+    AVFrameSideData *sd;
+    if (!frame || !buf || !size)
+        return 0;
+    sd = av_frame_new_side_data(frame, AV_FRAME_DATA_VEYE_BLOCKINFO, size);
+    if (!sd)
+        return AVERROR(ENOMEM);
+    memcpy(sd->data, buf, size);
     return 0;
 }
diff --git a/libavcodec/veye_blockinfo.h b/libavcodec/veye_blockinfo.h
index eadfb7b..24f18be 100644
--- a/libavcodec/veye_blockinfo.h
+++ b/libavcodec/veye_blockinfo.h
@@ -21,7 +21,17 @@
 
 /* 'V','E','B','K' stored little-endian -> raw bytes read as "VEBK". */
 #define VEYE_BI_MAGIC   0x4B424556u
-#define VEYE_BI_VERSION 1
+#define VEYE_BI_VERSION 12
+
+/* H.264 per-MB scratch stride (int32 per MB) in the decoder's veye_mb_bits
+ * table: [0]=total [1]=prediction [2]=transform bits, [3]=intra type
+ * (0 inter/skip, 1 I_NxN, 2 I_16x16, 3 PCM), [4]=representative luma intra mode
+ * (-1 if not intra), [5]=slice id, [6..21]=canonical luma intra mode of each of
+ * the 16 4x4 luma blocks (H.264 block scan order; -1 if not intra). */
+#define VEYE_H264_MB_STRIDE 22
+
+/* Max reference POCs recorded per list in the appended ref section. */
+#define VEYE_MAX_REFS 16
 
 typedef struct VeyeBlockInfoHeader {
     uint32_t magic;       ///< VEYE_BI_MAGIC
@@ -39,6 +49,57 @@ typedef struct VeyeBlockRecord {
     int32_t  qp;          ///< quantizer for the cell, or -1 if unknown
 } VeyeBlockRecord;
 
+/*
+ * AV1 cells are emitted at min mode-info (4x4) granularity. A coding block
+ * spans a variable number of these cells per its bsize; the consumer collapses
+ * equal-block cells the way HEVC collapses by cu_log2. Dispatched on
+ * codec_id == AV1. QP carries libaom's per-block current_qindex (0..255);
+ * motion vectors are in 1/8-pel luma units (col=x, row=y).
+ */
+typedef struct VeyeBlockRecordAV1 {
+    uint8_t  bsize;       ///< libaom BLOCK_SIZE enum (0..BLOCK_SIZES_ALL-1)
+    uint8_t  pred;        ///< PredType: 1 intra, 2 inter (AV1 has no skip/pcm class)
+    uint8_t  mode;        ///< libaom PREDICTION_MODE
+    uint8_t  skip;        ///< skip_txfm flag (no residual)
+    int32_t  qp;          ///< current_qindex for the block (0..255)
+    int16_t  mv0_x;       ///< L0 motion vector, 1/8-pel luma
+    int16_t  mv0_y;
+    int16_t  mv1_x;       ///< L1 motion vector, 1/8-pel luma (compound only)
+    int16_t  mv1_y;
+    int8_t   ref0;        ///< ref_frame[0] (0=INTRA, 1..7 inter, -1 none)
+    int8_t   ref1;        ///< ref_frame[1] (-1 if single-reference)
+    uint8_t  tx_size;     ///< libaom TX_SIZE
+    uint8_t  palette;     ///< luma palette size (0 = none, 2..8 colors)
+} VeyeBlockRecordAV1;
+
+struct veye_insp_frame_data;
+
+/**
+ * Serialize a libaom inspection frame (4x4 MI grid) into
+ * AV_FRAME_DATA_VEYE_BLOCKINFO and attach it to @p frame. @p fd must already
+ * have been filled by ifd_inspect(). Returns 0 on success/skip,
+ * AVERROR(ENOMEM) on allocation failure.
+ */
+int ff_veye_export_blockinfo_av1(AVFrame *frame,
+                                 const struct veye_insp_frame_data *fd);
+
+/**
+ * Serialize a libaom inspection frame into a freshly allocated buffer (the same
+ * bytes ff_veye_export_blockinfo_av1 would attach). On success returns 0 and
+ * sets @p out_buf (av_malloc'd; caller frees with av_free) and @p out_size;
+ * sets *out_buf=NULL with return 0 when there is nothing to emit. This lets the
+ * decoder buffer each decoded frame's block info keyed by order hint and attach
+ * the matching one when AV1 output order differs from decode order.
+ */
+int ff_veye_serialize_blockinfo_av1(const struct veye_insp_frame_data *fd,
+                                    uint8_t **out_buf, size_t *out_size);
+
+/**
+ * Attach a pre-serialized VEYE block-info payload to @p frame. Returns 0 on
+ * success/skip, AVERROR(ENOMEM) on allocation failure.
+ */
+int ff_veye_attach_blockinfo(AVFrame *frame, const uint8_t *buf, size_t size);
+
 /*
  * HEVC cells are emitted at min-CB (typically 8x8) granularity. Unlike the
  * fixed-size H.264 macroblock, an HEVC coding unit spans a variable number
@@ -49,10 +110,70 @@ typedef struct VeyeBlockRecordHEVC {
     uint8_t  cu_log2;     ///< log2 of the luma CU size in pixels (3..6)
     uint8_t  pred;        ///< PredType: 1 intra, 2 inter, 3 skip, 4 pcm
     uint8_t  intra_mode;  ///< luma intra prediction mode 0..34 (intra only)
-    uint8_t  reserved;
+    uint8_t  pred_flag;   ///< HEVC PredFlag: 0 intra, 1 L0, 2 L1, 3 BI
     int32_t  qp;          ///< luma QP for the cell
+    int16_t  mv0_x;       ///< L0 motion vector, 1/4-pel luma (valid if PF_L0)
+    int16_t  mv0_y;
+    int16_t  mv1_x;       ///< L1 motion vector, 1/4-pel luma (valid if PF_L1)
+    int16_t  mv1_y;
+    int8_t   ref0;        ///< L0 reference index, -1 if unused
+    int8_t   ref1;        ///< L1 reference index, -1 if unused
+    uint8_t  part_mode;   ///< HEVC PartMode (0=2Nx2N,1=2NxN,2=Nx2N,3=NxN,4..7 AMP)
+    uint8_t  ct_depth;    ///< CU coding-tree depth (0 = CTB, +1 per quadtree split)
 } VeyeBlockRecordHEVC;
 
+/*
+ * VideoEye v4 (HEVC): after the per-min-CB records a luma transform-unit size
+ * grid is appended at min-TB granularity: a VeyeTuHeader followed by
+ * tu_grid_w * tu_grid_h bytes, each the log2 luma TU size covering that cell
+ * (0 = no explicit TU, i.e. use the CU size). Chroma TUs are derived by the
+ * consumer. This sub-grid is independent of the coarser min-CB record grid.
+ */
+typedef struct VeyeTuHeader {
+    uint32_t tu_grid_w;   ///< min-TB cells across
+    uint32_t tu_grid_h;   ///< min-TB cells down
+    uint32_t tu_unit;     ///< pixels per min-TB cell (e.g. 4)
+} VeyeTuHeader;
+
+/*
+ * VideoEye v5 (HEVC): after the TU grid, a fixed-size reference section gives
+ * this frame's own POC and its L0/L1 reference picture POCs (last slice's
+ * lists). The consumer maps POCs back to frame indices to mark references on
+ * the frame chart.
+ */
+typedef struct VeyeRefHeader {
+    int32_t own_poc;              ///< this frame's POC
+    int32_t nb_l0;               ///< number of valid L0 refs
+    int32_t nb_l1;               ///< number of valid L1 refs
+    int32_t l0_poc[VEYE_MAX_REFS];  ///< L0 reference POCs
+    int32_t l1_poc[VEYE_MAX_REFS];  ///< L1 reference POCs
+} VeyeRefHeader;
+
+/*
+ * VideoEye v6 (HEVC): after the reference section, a picture-structure section
+ * describes slice and tile partitioning. A VeyeStructHeader is followed by:
+ *   - int32  slice_id[ctb_width * ctb_height]   (CTB raster; -1 = undecoded)
+ *   - uint32 col_bd_px[num_tile_cols + 1]       (tile column boundaries, px)
+ *   - uint32 row_bd_px[num_tile_rows + 1]       (tile row boundaries, px)
+ * Slice boundaries are where adjacent CTBs carry a different slice_id; tile
+ * boundaries are the col/row pixel positions. (num_tile_* == 1 means no tiles.)
+ */
+typedef struct VeyeStructHeader {
+    uint32_t ctb_size;       ///< luma CTB size in pixels (1 << log2_ctb_size)
+    uint32_t ctb_width;      ///< CTBs across
+    uint32_t ctb_height;     ///< CTBs down
+    uint32_t num_tile_cols;  ///< number of tile columns (>= 1)
+    uint32_t num_tile_rows;  ///< number of tile rows (>= 1)
+} VeyeStructHeader;
+
+/*
+ * VideoEye v7 (HEVC): after the picture-structure section, three int32 min-CB
+ * grids give each CU's coded bit cost -- cu_bits (total), pu_bits (prediction
+ * syntax), tu_bits (residual) -- stored at the CU's top-left min-CB (other
+ * cells are unused; the consumer reads at CU origins). Each grid is
+ * min_cb_width * min_cb_height int32 values in raster order.
+ */
+
 /**
  * Serialize a per-cell grid into AV_FRAME_DATA_VEYE_BLOCKINFO side data and
  * attach it to @p frame. Reads are indexed with @p grid_stride; output is
@@ -61,6 +182,12 @@ typedef struct VeyeBlockRecordHEVC {
  */
 int ff_veye_export_blockinfo(AVFrame *frame, enum AVCodecID codec_id,
                              const uint32_t *mb_type, const int8_t *qscale,
+                             const int32_t *mb_bits,
+                             int16_t (*const motion_val[2])[2],
+                             int8_t *const ref_index[2],
+                             int own_poc,
+                             const int32_t *l0_poc, int nb_l0,
+                             const int32_t *l1_poc, int nb_l1,
                              int grid_w, int grid_h, int grid_stride,
                              int block_unit);
 
@@ -74,7 +201,11 @@ int ff_veye_export_blockinfo(AVFrame *frame, enum AVCodecID codec_id,
  * frame. Records are zero-initialized. Returns 0 on success/skip, ENOMEM else.
  */
 int ff_veye_alloc_blockinfo_hevc(AVFrame *frame, int min_cb_width,
-                                 int min_cb_height, int log2_min_cb_size);
+                                 int min_cb_height, int log2_min_cb_size,
+                                 int min_tb_width, int min_tb_height,
+                                 int log2_min_tb_size,
+                                 int ctb_width, int ctb_height,
+                                 int num_tile_cols, int num_tile_rows);
 
 /**
  * Serialize an HEVC min-CB grid into AV_FRAME_DATA_VEYE_BLOCKINFO. The HEVC
@@ -83,26 +214,50 @@ int ff_veye_alloc_blockinfo_hevc(AVFrame *frame, int min_cb_width,
  * pre-allocated by ff_veye_alloc_blockinfo_hevc() (filling it in place) when
  * one is already attached, otherwise allocates a fresh one.
  *
- * @p mvf_pred_flag points at the pred_flag field of the frame's first
- * MvField; @p mvf_elem_size is sizeof(MvField), so the field is sampled
- * without this module depending on the HEVC struct layout. pred_flag == 0
- * (PF_INTRA) marks an intra block.
+ * @p tab_mvf points at the frame's MvField array (one per min-PU); each
+ * record carries mv[2], ref_idx[2] and pred_flag. @p mvf_field_off gives the
+ * byte offsets of those members and @p mvf_elem_size is sizeof(MvField), so
+ * the motion field is read without this module depending on the HEVC struct
+ * layout. pred_flag == 0 (PF_INTRA) marks an intra block. Each min-CB samples
+ * the MvField of its top-left min-PU.
  *
  * All min-CB tables (skip_flag, tab_ct_depth, qp_y_tab) are strided by
  * @p min_cb_width; the min-PU tables (tab_ipm, is_pcm, MvField) by
  * @p min_pu_width. Returns 0 on success/skip, AVERROR(ENOMEM) on failure.
  */
+typedef struct VeyeMvFieldLayout {
+    size_t mv0_off;   ///< offsetof(MvField, mv[0])  (Mv = {int16 x, int16 y})
+    size_t mv1_off;   ///< offsetof(MvField, mv[1])
+    size_t ref_off;   ///< offsetof(MvField, ref_idx) (int8_t[2])
+    size_t flag_off;  ///< offsetof(MvField, pred_flag) (int8_t)
+} VeyeMvFieldLayout;
+
 int ff_veye_export_blockinfo_hevc(AVFrame *frame,
                                   const uint8_t *skip_flag,
                                   const uint8_t *tab_ct_depth,
+                                  const uint8_t *tab_part_mode,
                                   const int8_t  *qp_y_tab,
                                   const uint8_t *tab_ipm,
                                   const uint8_t *is_pcm,
-                                  const int8_t  *mvf_pred_flag,
+                                  const void   *tab_mvf,
                                   size_t mvf_elem_size,
+                                  const VeyeMvFieldLayout *mvf_layout,
+                                  const uint8_t *tab_tu_log2,
+                                  int own_poc,
+                                  const int32_t *l0_poc, int nb_l0,
+                                  const int32_t *l1_poc, int nb_l1,
+                                  const int32_t *tab_slice_address,
+                                  int ctb_width, int ctb_height,
+                                  int num_tile_cols, int num_tile_rows,
+                                  const unsigned int *col_bd,
+                                  const unsigned int *row_bd,
+                                  const int32_t *tab_cu_bits,
+                                  const int32_t *tab_pu_bits,
+                                  const int32_t *tab_tu_bits,
                                   int min_cb_width, int min_cb_height,
                                   int min_pu_width,
+                                  int min_tb_width, int min_tb_height,
                                   int log2_min_cb_size, int log2_ctb_size,
-                                  int log2_min_pu_size);
+                                  int log2_min_pu_size, int log2_min_tb_size);
 
 #endif /* AVCODEC_VEYE_BLOCKINFO_H */
